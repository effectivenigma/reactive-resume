export function DefaultTheme() {
  let styles: ITheme = {
    name: "default",
    palette: null,
    typography: {
      fontSize: "14px",
      fontFamily: "sans-serif"
    }
  };

  return {
    styles
  }
}